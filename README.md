# 379 Machine Learning Project

## setup
All code can be found in the /src folder. To install dependencies enter the following in a terminal:
```
pip install -r requirements.txt
```

To run main:
```
python3 src/main.py 
```
